"""Static and dynamic analysis of the input image.

Submodules are imported directly rather than re-exported
here, so that low-level modules can depend on this package
without pulling in the translator.
"""
